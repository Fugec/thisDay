# Secrets — never commit
.secrets
.env
.env.*
*.env
client_secret*.json
*credentials*.json
*.token.json

# Local documentation — personal notes, not for the repo
documentation/
documentation/todo-remaining.txt
CLAUDE.md
*.md
CONFIGURATION.txt

# Tests — local only, not deployed or committed
tests/
package.json
youtube-upload/test-*.js
!youtube-upload/test-video-text.js

# Wrangler build artifacts
.wrangler/
.wrangler-xdg/
node_modules/
node_modules/.mf/
.dev.vars

# Ignore generated/older blog HTML files so edits don't get committed
# Adjust this pattern if you want to keep specific posts tracked.
/blog/**/index.html

# Local Ancient Origins article workspace
/ancient-origins-daorson/
/scripts/humanize-article.mjs
